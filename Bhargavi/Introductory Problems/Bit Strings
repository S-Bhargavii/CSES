#include <iostream>
#include <algorithm>
using namespace std;
#define ll long long

int main()
{
    int n;
    cin >> n;
    ll s = 1;
    for (int i = 0; i < n; i++)
    {
        s = s * 2 % ((int)1e9 + 7);
    }
    cout << s;
}
